import { Container } from "src/Globals/Styles/main";
import styled from "styled-components";

let BorderBlackContainer = styled(Container)`
    border:2px solid black;
    background:white;
    font-family:"IBM Plex Sans",sans-serif;
    font-weight:bold;
`